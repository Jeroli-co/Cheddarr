{
  "name": "client",
  "version": "0.1.0",
  "scripts": {
    "postinstall": "cd client && npm install",
    "build": "cd client && npm run build",
    "test": "cd client && npm run test"
  },
  "dependencies": {
    "js-cookie": "^2.2.1"
  }
}
